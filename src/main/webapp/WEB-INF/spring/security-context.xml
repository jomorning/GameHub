<?xml version="1.0" encoding="UTF-8"?>

<beans:beans
	xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
	http://www.springframework.org/schema/security https://www.springframework.org/schema/security/spring-security.xsd
	http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd">

	<http use-expressions="true">

		<!-- 정적 리소스 허용 -->
		<intercept-url pattern="/resources/**" access="permitAll" />
		<intercept-url pattern="/upload/**" access="permitAll" />

		<!-- 로그인 허용 -->
		<intercept-url pattern="/login" access="permitAll" />
		<intercept-url pattern="/login/**" access="permitAll" />

		<!-- admin -->
		<intercept-url pattern="/user/**" method="PUT" access="hasRole('ADMIN')" />
		<intercept-url pattern="/user/**" method="DELETE" access="hasRole('ADMIN')" />
		<intercept-url pattern="/user/search" access="hasRole('ADMIN')" />
		
		<intercept-url pattern="/game/new" access="hasRole('ADMIN')" />
		<intercept-url pattern="/game/*/edit" access="hasRole('ADMIN')" />
		<intercept-url pattern="/game/**" method="POST" access="hasRole('ADMIN')" />
		<intercept-url pattern="/game/**" method="PUT" access="hasRole('ADMIN')" />
		<intercept-url pattern="/game/**" method="DELETE" access="hasRole('ADMIN')" />
		
		<!-- 로그인 사용자 -->
		<intercept-url pattern="/user/**" method="PUT" access="isAuthenticated()" />
		<intercept-url pattern="/user/**" method="DELETE" access="isAuthenticated()" />
		<intercept-url pattern="/user/**" access="permitAll" />
		
		<intercept-url pattern="/post/search" access="permitAll" />
		<intercept-url pattern="/post/new" access="isAuthenticated()" />
		<intercept-url pattern="/post/*/edit" access="isAuthenticated()" />
		<intercept-url pattern="/post/**" method="POST" access="isAuthenticated()" />
		<intercept-url pattern="/post/**" method="PUT" access="isAuthenticated()" />
		<intercept-url pattern="/post/**" method="DELETE" access="isAuthenticated()" />
		
		<intercept-url pattern="/game/search" access="permitAll" />
		<intercept-url pattern="/game/all" access="permitAll" />
		<intercept-url pattern="/game/**" access="permitAll" />

		<intercept-url pattern="/**" access="permitAll" />

		<form-login login-page="/login"
			login-processing-url="/login" default-target-url="/main?login=true"
			authentication-failure-url="/login/failed"
			username-parameter="userId" password-parameter="userPw" />

		<logout logout-url="/logout"
			logout-success-url="/login?logout=true" invalidate-session="true"
			delete-cookies="JSESSIONID" />

		<csrf />
	</http>

	<authentication-manager>
		<authentication-provider>
			<password-encoder ref="passwordEncoder" />
			<jdbc-user-service data-source-ref="dataSource"
				users-by-username-query="SELECT user_id, user_pw, 1 FROM app_user WHERE user_id = ?"
				authorities-by-username-query="SELECT user_id, user_role FROM app_user WHERE user_id = ?" />
		</authentication-provider>
	</authentication-manager>

	<beans:bean name="mvcHandlerMappingIntrospector"
		class="org.springframework.web.servlet.handler.HandlerMappingIntrospector" />

</beans:beans>